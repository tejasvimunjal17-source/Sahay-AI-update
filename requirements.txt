# Phase 7 requirements. Supabase, OpenRouter, export libraries, and
# bcrypt (admin auth) are all now active, so `pip install -r
# requirements.txt` today installs everything Phase 1-7 use.

streamlit>=1.38
python-dotenv>=1.0
supabase>=2.4

# --- Phase 3 (AI engine) ---
requests>=2.31
tenacity>=8.2

# --- Phase 6 (Export) ---
fpdf2>=2.7
python-docx>=1.1

# --- Phase 7 (Admin) ---
bcrypt>=4.1
